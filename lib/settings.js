const MONTH_IN_MS = 30 * 24 * 60 * 60 * 1000

const SETTING_TYPES = {
  currency: 'currency',
  installment: 'installment',
  fixed: 'fixed',
}

const ACTIONS = {
  edit: 'edit',
  delete: 'delete'
}

export {
  MONTH_IN_MS,
  SETTING_TYPES,
  ACTIONS
}